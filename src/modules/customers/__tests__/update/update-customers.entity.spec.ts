import { CustomersEntity } from '../../entities/customers.entity';

describe('UpdateCustomersEntity', () => {
  it('should be defined', () => {
    expect(new CustomersEntity()).toBeDefined();
  });
});